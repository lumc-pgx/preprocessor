rule fastq_to_fasta:
    """
    Convert fastq to fasta
    """
    input:
        rules.ccs_amplicon.output.fastq
    output:
        "preprocessor/CCS_Amplicon/haplotypes/{barcode}.fasta"
    conda:
        "../envs/fastq_to_fasta.yaml"
    shell:
        "fastools fq2fa {input} {output}"
